<Window x:Class="FE.GiaoDienDangNhap"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FE"
        mc:Ignorable="d"
        Title="GiaoDienDangNhap" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="ĐĂNG NHẬP"
                   FontWeight="Bold" FontSize="16"
                   HorizontalAlignment="Center"
                   Margin="0,5,0,15"
                   Grid.Row="0"/>

        <!-- Tên đăng nhập -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBlock Text="Tên đăng nhập" Width="100" VerticalAlignment="Center"/>
            <Image Source="/Images/user.png" Width="24" Height="24" Margin="5,0"/>
            <TextBox x:Name="txtTenDangNhap" Width="200" Height="28" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- Mật khẩu -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10">
            <TextBlock Text="Mật khẩu" Width="100" VerticalAlignment="Center"/>
            <Image Source="/Images/lock.png" Width="24" Height="24" Margin="5,0"/>
            <PasswordBox x:Name="txtMatKhau" Width="170" Height="28"/>
            <Button Content="👁" Width="30" Height="28" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Nút và link -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Đăng Nhập" Width="100" Height="35" Margin="5"/>
                <Button Content="Thoát" Width="100" Height="35" Margin="5"/>
            </StackPanel>

            <TextBlock Text="Bạn chưa có tài khoản?" 
                       Foreground="Blue"
                       TextDecorations="Underline"
                       Margin="0,15,0,0"
                       Cursor="Hand"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>

</Window>
